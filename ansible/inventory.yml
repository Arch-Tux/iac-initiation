all:
  hosts:
    vm_azure:
      ansible_host: "{{ lookup('pipe', 'cd .. && terraform output -raw public_ip_address') }}"
      ansible_user: azureuser
      # ansible_password défini dans group_vars/all.yml (gitignored)
      ansible_become: yes
      ansible_become_method: sudo
      ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
